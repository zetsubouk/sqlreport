{
  "name": "订单查询",
  "ds": "demo",
  "sql": "SELECT region AS 区域, product AS 产品, amount AS 金额, dt AS 日期\nFROM orders\nWHERE dt >= '{{d.begin}}'\n  AND dt <= '{{d.end}}'\n  AND region = '{{region}}'\nORDER BY dt",
  "params": [
    {"id": "d", "label": "日期范围", "type": "daterange", "options": "", "default": "2026-08-01"},
    {"id": "region", "label": "区域", "type": "select", "options": "华北,华东,华南", "default": ""}
  ]
}
